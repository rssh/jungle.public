<?xml version="1.0"?>
<project name="jabsorb disrtibution " default="build" basedir="."
    xmlns:ivy="antlib:fr.jayasoft.ivy.ant"
 >

 <property file="build.properties" />

 <property name="ivy.install.version" value="2.0.0-rc2" />
 <condition property="ivy.home" value="${env.IVY_HOME}">
        <isset property="env.IVY_HOME" />
 </condition>
 <property name="ivy.home" value="${user.home}/.ivy" />
 <property name="ivy.jar.dir" value="${ivy.home}/jars" />
 <property name="ivy.jar.file" value="${ivy.jar.dir}/ivy.jar" />
 <property name="jsch.version" value="0.1.41" />
 <property name="jsch.jar.file" value="${ivy.jar.dir}/jsch.jar" />

 <condition property="without.downloads">
  <or>
   <and>
    <available file="${ivy.jar.dir}/ivy.jar" />
    <available file="${ivy.jar.dir}/jsch.jar" />
    <available file="${ivy.jar.dir}/ivy.build.plugin-1.0.jar" />
   </and>
   <isset property="offline" />
  </or>
 </condition>

	
 <target name="download-ivy" unless="without.download">
    <mkdir dir="${ivy.jar.dir}"/>
    <!-- download Ivy from web site so that it can be used even without any special installation -->
    <get src="http://repo1.maven.org/maven2/org/apache/ivy/ivy/${ivy.install.version}/ivy-${ivy.install.version}.jar"
         dest="${ivy.jar.file}" usetimestamp="true"/>
    <get src="http://prdownloads.sourceforge.net/jsch/jsch-${jsch.version}.jar?download"
         dest="${jsch.jar.file}" usetimestamp="true"/>
 </target>
	
 <target name="declare-ivy" depends="download-ivy" >
   <path id="ivy.lib.path">
      <fileset dir="${ivy.jar.dir}" includes="*.jar"/>
   </path>
   <taskdef resource="fr/jayasoft/ivy/ant/antlib.xml"
            uri="antlib:fr.jayasoft.ivy.ant" 
            classpathref="ivy.lib.path"/>
 </target>


  <target name="clean-ivy" description="--> clean the ivy installation">
    <delete dir="${ivy.jar.dir}"/>
  </target>

 <target name="init-ivy" depends="declare-ivy">
  <ivy:resolve />
 </target>

 <target name="retrieve-src" depends="init-ivy">
  <ivy:retrieve pattern="origin.sources/[artifact]-[revision].[ext]" conf="withdep" />
 </target>

 <target name="unzip-src" depends="retrieve-src">
  <unzip src="origin.sources/hessian-src-${origin.version}.jar" 
         dest="origin.sources" />
 </target>

 <target name="patch-src" depends="unzip-src">
 <!--
  <patch patchfile="patches/patch.2" strip="0" />
 -->
 </target>

 <target name="build" depends="patch-src">
  <javac  srcdir="origin.sources" destdir="build"  > 
    <classpath>
      <pathelement location="origin.sources/servlet-api-${servlet-api.version}.jar" />
    </classpath>
  </javac>
  <mkdir dir="build/META-INF" />
  <copy file="origin.sources/META-INF/MANIFEST.MF" todir="build/META-INF" />
  <jar destfile="artifacts/hessian-${patch.version}.jar" 
       basedir="build"
  >
  </jar>
 </target>

 <target name="clean">
  <delete dir="build/META-INF" />
  <delete dir="build/com" />
 </target>

 <target name="clean-all" depends="clean">
  <delete>
   <fileset dir="artifacts" includes="*.jar" />
   <fileset dir="artifacts" includes="*.xml" />
  </delete>
  <delete dir="origin.sources/META-INF" />
  <delete dir="origin.sources/com" />
  <delete dir="origin.sources" includes="*.jar" />
  <delete dir="origin.sources" includes="*.license" />
 </target>


 <target name="distributives" depends="build">
 </target>

   <target name="publish" depends="distributives">
     <ivy:publish resolver="datacenter.gradsoft.ua" 
                  pubrevision="${patch.version}" 
                  overwrite="true" 
                  >
       <artifacts pattern="artifacts/[artifact]-[revision].[ext]" />
     </ivy:publish>
    </target>

   <target name="publish.local" depends="distributives">
     <ivy:publish resolver="jungle.repo" pubrevision="${patch.version}" 
                  overwrite="true" 
                  >
       <artifacts pattern="artifacts/[artifact]-[revision].[ext]" />
     </ivy:publish>
    </target>


</project>
